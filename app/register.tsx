import { Colors } from "@/constants/Colors";
import { Text, View } from "react-native";

export default function Register(){
    return(
        <View>
            <Text style={{color: Colors.Astronaut[400]}}>Tela de cadastro</Text>
        </View>
    )
}